<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude app preferences that should not be backed up -->
    <exclude domain="sharedpref" path="." />

    <!-- Include document data -->
    <include domain="file" path="." />
</full-backup-content>